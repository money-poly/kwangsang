import 'package:flutter/material.dart';

class LimitStockView extends StatelessWidget {
  const LimitStockView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LimitStockView'),
      ),
    );
  }
}
