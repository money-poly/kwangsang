import 'package:flutter/material.dart';

class BestProductView extends StatelessWidget {
  const BestProductView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BestProductView'),
      ),
    );
  }
}
